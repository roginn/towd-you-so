# Towd You So — Frontend

## Development

```bash
npm install
npm run dev
```

## Testing

Tests use [Vitest](https://vitest.dev/) and cover pure functions and logic (no component/DOM tests).

```bash
# Watch mode (re-runs on file changes)
npm test

# Single run (CI-friendly)
npm test -- --run
```

## Build

```bash
npm run build
```
